/**
 * @satisfies {import("prettier").Config}
 * @see https://prettier.io/docs/en/configuration.html
 */
const config = {};

// We provide an empty config to accept the defaults and avoid Prettier climbing
// up to some user-level config
export default config;
